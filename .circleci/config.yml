version: 2
jobs:
  build:
    working_directory: ~/
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: |
          cd '..'
          cd '..'
          node validator.js
