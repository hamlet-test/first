version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: |
          taskName=$CIRCLE_BRANCH
          if [[ -d $taskName ]] && [[ $taskName != _* ]] && [[ $taskName != .* ]]
          then
            node validator.js
          fi
